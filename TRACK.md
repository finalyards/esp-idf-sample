# Track

Releases of the following, so we can remove the `patch` section in `Cargo.toml`, at some point:

- `embuild` 0.33.1
- `esp-idf-sys` > 0.37.2
- `esp-idf-hal` > 0.46.2
- `esp-idf-svc` > 0.52.1

- [ ] Remove `[patch.crates-io]` once releases have rolled out.
